# Building a validator

<Note>We're working to release the module boilerplate early next week.</Note>

This tutorial will walk you through building a simple validator module. To learn more about validators, check out the [validators](/modules/validators) section of the docs.

## Installation

1. Clone [this repo](https://github.com/rhinestonewtf/module-boilerplate) and install dependencies:

```bash
git clone git@github.com:rhinestonewtf/module-boilerplate.git
cd module-boilerplate
forge install
```

<Note>
	If you do not have foundry installed, run the following command and follow the onscreen instructions: ```bash curl
	-L https://foundry.paradigm.xyz | bash ```
</Note>

## Building the validator

In this section, we will walk you through building a simple validator module.

2. Open the file `src/modules/validators/ValidatorTemplate.sol` and rename it to `SimpleValidator.sol`.

3. Edit the function `validateUserOp` to the following code:

```solidity
function validateUserOp(
    UserOperation calldata userOp,
    bytes32 userOpHash
)
    external
    view
    virtual
    returns (uint256)
{
    // TODO
    return VALIDATION_SUCCESS;
}
```

## Testing the validator

In this section, we will walk you through testing the validator module that you just built.

4. Create a new file in `test/modules/validators/SimpleValidator.t.sol`.

5. Add the following code:

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

import { SafeIntegrationBoilerplate } from "../utils/setup/SafeIntegrationBoilerplate.t.sol";

/// @title SimpleValidator
contract SimpleValidator is SafeIntegrationBoilerplate {

    function setUp() public override {
        super.setUp();
    }

    // TODO

}

```

6. Run `forge test` to run the test and ensure that it passes.
