import { Tabs } from "nextra/components";

# Hook MultiPlexer

The Hook MultiPlexer module is a module that allows to add multiple hooks to a smart account.

You can find the source code for the module in the [Core Modules repo](https://github.com/rhinestonewtf/core-modules/tree/main/src/HookMultiPlexer) and the deployment address in the [address book](../../overview/address-book.mdx).

## `getInstallHookMultiPlexer`

The `getInstallHookMultiPlexer` function is used to create an instance of the Hook MultiPlexer Hook module. This function returns a module object.

<Tabs items={["Safe", "Kernel"]}>

<Tabs.Tab>

```typescript
import { getInstallHookMultiPlexer } from "@rhinestone/module-sdk";

enum HookType {
  GLOBAL = 0,
  SIG = 1,
}

const params = {
  globalHooks: ['0x...', '0x...'], // hook addresses
  valueHooks: []
  delegatecallHooks: []
  sigHooks: [
    {
      sig: '0x...', // 4 byte hex string
      subHooks: '0x...' // hook address
    }
  ],
  targetHooks: []
  selector: '0x',
  hookType: HookType.GLOBAL
}

const hookMultiPlexer = getInstallHookMultiPlexer(params);
```

</Tabs.Tab>

<Tabs.Tab>
```typescript
import { getInstallHookMultiPlexer } from "@rhinestone/module-sdk";

const params = {
  globalHooks: ['0x...', '0x...'], // hook addresses
  valueHooks: []
  delegatecallHooks: []
  sigHooks: [
    {
      sig: '0x...', // 4 byte hex string
      subHooks: '0x...' // hook address
    }
  ],
  targetHooks: []
}

const hookMultiPlexer = getInstallHookMultiPlexer(params);
```

</Tabs.Tab>
</Tabs>

## `getHooks`

The `getHooks` function is used to get sub hooks installed in hook multiPlexer. This function returns an array of hook addresses.

```typescript
import { getHooks } from "@rhinestone/module-sdk";

const hooks = await getHooks({
  account,
  client
});
```

## `getAddHookAction`

The `getAddHookAction` function is used to add a sub hook to the hook multiPlexer module. This function returns an execution object.

```typescript
import { getAddHookAction } from "@rhinestone/module-sdk";

enum HookType {
  GLOBAL,
  DELEGATECALL,
  VALUE,
  SIG,
  TARGET,
}

const addHookAction = getAddHookAction({
  hook: '0x...', // hook address
  hookType: HookType.GLOBAL,
  sig: '0x...', // 4 byte hex string required for installing SIG hook
});
```

## `getRemoveHookAction`

The `getRemoveHookAction` function is used to remove a sub hook from the hook multiPlexer module. This function returns an execution object.

```typescript
import { getRemoveHookAction } from "@rhinestone/module-sdk";

enum HookType {
  GLOBAL,
  DELEGATECALL,
  VALUE,
  SIG,
  TARGET,
}

const removeHookAction = getRemoveHookAction({
  hook: '0x...', // hook address
  hookType: HookType.GLOBAL,
  sig: '0x...', // 4 byte hex string required for installing SIG hook
});
```

## `HOOK_MULTI_PLEXER_ADDRESS`

The `HOOK_MULTI_PLEXER_ADDRESS` is a constant that represents the module address.
