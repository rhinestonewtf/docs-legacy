# findModule

Find a deployed module on the Registry.

## Usage

```solidity copy
ModuleRecord memory moduleRecord = findModule({ moduleAddress: moduleAddress });
```

## Parameters

### moduleAddress

- Type: `address`

The address of the module to find.

## Returns

### moduleRecord

- Type: `ModuleRecord`

The module record.
